import 'package:flutter/material.dart';

class ZapatillasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Zapatillas'),
      ),
      body: Center(
        child: Text('Contenido de la página de Zapatillas'),
      ),
    );
  }
}
