import 'package:flutter/material.dart';

class RopaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ropa'),
      ),
      body: Center(
        child: Text('Contenido de la página de Ropa'),
      ),
    );
  }
}
